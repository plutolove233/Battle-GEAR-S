你是 Battle-GEAR-S（机斗战甲，Godot 4.6 回合制战棋）的装备牌效果实现工程师，接力上个 session 继续把装备牌 067-126 的效果落地为可运行代码 + 测试，从套装12高机动装一路做到套装21帝国的神莺（126），全程保持 run_tests 全绿、无回归。要快：批量推进，减少往返确认。

# 当前进度（套装1-11 已完成，全绿 290 测试）
装备牌 001-066（套装1-11）已落码完成，run_tests 290 PASS 全绿。已实现 effect_001-049 + 089（复用/新建），含已就位的新机制：CHOOSE_INTEGER（Expression 解析 $choice.n/$binding_context.owner_gold/算术/floor + AI自动选min + 人类 choice_panel 列整数选项弹窗 UI + resume_pending_effect choose_integer phase）、EXECUTE_DAMAGE_CHANGE fixed_slot（直接置X点跳过转移窗）、MODIFY_ATTACK_MIGHT attack_action_id 定位、Q3 守卫（置损伤致来源牌弃置后剩余动作停止，只 fixed_slot damage 后启用）、power_spent_this_turn 追踪（MechState 字段 + spend_power 更新 + basic_move 注入 + TurnService 重置）、binding_context 含 slot_id（_register_equipment_effects 签名加 slot_id）、派生值阈值扩展（card_damage_immune_armor_amount 加 046/049、slot_damage_threshold_power_bonus 加 048）。
下一步从套装12(067)做到套装21(126)，共60张/10套装。落码依据 new_logic/装备牌效果拆解_067-126.txt（60张/10套装，逐张含效果拆解+实现预告+顺序与坑+测试场景+歧义点▶回答行；**头部 L19-24 有「新增/扩展总览」必读**）。

# 立即开始：先读这些（有记忆就用记忆，别重复读代码）
- 本提示词全文。
- 记忆（C:\Users\m1396\.claude\projects\f--Battle-GEAR-S\memory\）：
  - MEMORY.md（索引）
  - equipment-suite-11-progress.md（套装11完成 + 整套001-066完成 + 遗留清单，最重要）
  - equipment-suite-10-progress.md（CHOOSE_INTEGER 实现细节 + _eval_expr + choose_integer phase + UI + GOLD_ABOVE改binding_context查金币 + restore_power str）
  - equipment-suite-9-progress.md（fixed_slot + attack_action_id + Q3守卫 + binding_context加slot_id + USED_ACTION_HAS_LINKED_ATTACK）
  - equipment-suite-5-progress.md（**动力消耗走 spend_power 直调不发 STAT_MOD_SETTLE 架构事实——067 effect_050 监听 STAT_MOD_SETTLE 必读**）
  - equipment-suite-6-progress.md（free_move/max_cells/adjacent_only——067-126 的 EXECUTE_SINGLE_MOVE 扩展必读）
  - equipment-suite-8-progress.md（effect_033/034 + effect_fire DIRECT + SET_EQUIP_IS_SELF + DRAW_EQUIPMENT_AND_IMMEDIATELY_SET——套装15 DRAW_EQUIPMENT_AND_CHOOSE_SET_OR_SELL 仿此）
  - equipment-suite-3-4-progress.md（派生值接入点 card_damage_immune_armor_amount/slot_damage_threshold_power_bonus——套装20全场光环接入必读）
  - equipment-damage-rules-clarification.md（4条规则，下方已内联）
  - equipment-fullset-redesign.md（总工程 + 既有 effect_id 映射 + JSON 手维护原则）
  - effect-coder-agent-3step.md（拆解-only 分工 + 外部提示词）
- new_logic/装备牌效果拆解_067-126.txt：先读头部 L1-25（新增/扩展总览），再按套装读。grep `套装1[2-9]|套装2[0-1]|^\[装备牌 0(67|73|79|85|91|97)|^\[装备牌 1(03|09|15|21)` 定位边界。
- .claude/agents/effect-coder.md 的「效果组成与运行逻辑」「关键既有约定与坑」「文件落点」三章。
- new_logic/装备牌效果拆解_外部模型提示词.md 的「二、既有件清单」（时点/条件op/目标规则/原子动作/已实现 effect_id 全清单——**注意此清单是 001-066 时的，067-126 新增的 op/动作/effect_id 以拆解_067-126.txt 为准**）。
- 权威规则文档（冲突时以此为准）：new_logic/机斗战甲规则书.txt、new_logic/各动作的生命周期与时点.txt、new_logic/行动牌的效果与逻辑.txt。
读完后直接干，不必汇报开场。

# 067-126 概览（落码前一次性读完拆解头部 L19-24 + 各套装首张）
**effect_id 编号**：新建 050-088、090-091（跳过已占用的 089）；复用 001-049、089 + 跨套装复用 021/023/005/033/034/039/044/048/019/032/026/027/055/058/074。

**10套装（effect_id 分配，细节读拆解）**：
- 套装12 高机动装 067-072：050(耗尽动力回复3+移动) / 051(被攻击弃2牌动力+6) / 052+053(躯干双入口) / 054 + 复用021
- 套装13 狙击影装 073-078：055 / 023+056 / 057 / 058 / 059 / 060
- 套装14 近战特装 079-084：061 / 062 / 063 / 064 + 复用005
- 套装15 王牌装 085-090：033+056 / 033+034 / 033+065（设置诱发抽牌，复用套装8）+ **DRAW_EQUIPMENT_AND_CHOOSE_SET_OR_SELL**（新复杂交互）
- 套装16 联邦的圣牛 091-096：066 / 067 / 068 / 069 + 复用039
- 套装17 帝国的雄鹰 097-102：070 / 071+072（**CHOOSE_INTEGER 金币换移动格数 + EXECUTE_SINGLE_MOVE.ignore_power/max_cells=$variables.n**）
- 套装18 轰雷装 103-108：055+074 / 075 + 复用048/019/032/058/026/027（多效果复合）
- 套装19 极电装 109-114：076 / 077 / 078 + 复用021/005
- 套装20 联邦的一角兽 115-120：080(**全场阵营光环**派生值) / 081 / 082 / 083 / 084(**REPEAT_SELF_DAMAGE_AND_FREE_MOVE** AVAILABILITY串行循环)
- 套装21 帝国的神莺 121-126：086 / 087+088(**虚拟武器**装备牌当武器不占槽) / 090 / 091 + 复用082

# 新机制清单（头部 L19-24，落码前给用户看方案，同类合并确认）
1. **新复杂交互动作**（TimingEngine._execute_actions 拦截）：
   - DRAW_EQUIPMENT_AND_CHOOSE_SET_OR_SELL（套装15）：仿 DRAW_EQUIPMENT_AND_IMMEDIATELY_SET（套装8已实现），增加卖出分支（discard reason=sell + GAIN_GOLD，按全局每回合卖出2张计数）。
   - REPEAT_SELF_DAMAGE_AND_FREE_MOVE（套装20 effect_084）：AVAILABILITY 响应攻击，首轮强制+之后每轮询问，仿 EXECUTE_DAMAGE_CHANGE + EXECUTE_SINGLE_MOVE 串行循环。
   - CHOOSE_INTEGER 复用/扩展（套装17金币换移动）：套装10已实现，复用 bind_as/$choice.n/_eval_expr/choose_integer phase/人类choice_panel弹窗。
2. **既有动作扩展**：
   - DRAW_ACTION.bind_result_to（把抽到的牌id绑定到变量供后续动作用）
   - EXECUTE_SINGLE_MOVE.ignore_power/force_exact_cells/max_cells=$variables.n（套装17：最多移动n格不扣动力；仿套装6 free_move/max_cells，新加 ignore_power 不依赖动力 + force_exact_cells 强制走满）
   - SPEND_POWER.amount=&"ALL_CURRENT"（消耗全部当前动力）
   - CHOOSE_ONE.auto_select_single（仅1个可用option时自动选不弹窗）
3. **新派生helper**（GeneratedEquipmentEffects.gd）：
   - 本机甲阵营装备计数（仿 compute_faction_armor_bonus/compute_faction_power_bonus）
   - 全场阵营光环 get_global_faction_equipment_aura_bonus（套装20 effect_080：接入 MechSlotState.get_effective_armor，影响全场所有机甲，**避免递归**——光环只加牌面提供值不再次触发派生统计）
   - 装备牌虚拟武器 get_virtual_weapon_from_equipment（套装21 effect_087：装备牌当虚拟武器不占武器槽，接入武器选择/ATTACK_BEFORE，requires_current_power_above=0）
4. **新状态 CANNOT_RESTORE_POWER**（仅拦截 method=restore 的 restore_power，默认下个我方 TURN_BEFORE_START 移除）
5. **新条件 op**（按拆解，067 effect_050 用 SELF_MECH_IS_STAT_TARGET/PAYLOAD_STAT_TYPE_IS/PAYLOAD_STAT_METHOD_IS + OWNER_POWER_EQUALS——但注意 STAT_MOD_SETTLE 监听问题，见坑）

# 4 条权威规则（用户拍板，落码遵守，继承001-066）
1. 损伤≥耐久立即弃置 + 进弃牌堆后不再执行效果：装备牌每次有损伤设置后检查损伤≥耐久则立即弃置；进弃牌堆的装备牌不再执行效果（例外："弃置时才发动"的离场效果由 DISCARD_AFTER 触发）。
2. 损伤设置方式：维修/攻击损伤是逐个设置且优先有装备牌处；但"设置X损伤到此牌/区域上"的效果是直接置X点（即使该区域无装备牌也设，EXECUTE_DAMAGE_CHANGE fixed_slot 走此路，套装9已实现）。
3. 装备牌效果晚于同优先级行动牌效果（TimingEngine 已有 tier/seat tiebreak，落码无需改）。
4. 备用区装备牌：不对其他玩家显示信息，无效果（除非注明）；只有正面设置的装备牌才触发效果。

# 注意事项（同样重要）
- 先 PvP 人类玩家 + UI：逻辑/UI 要对任意数量人类玩家可复用；AI 逻辑暂不管（但 AI 触发的装备效果不能挂死，可自动选最小可用）。
- 不做多余阅读和动作：有记忆就用记忆，立即开始；记得存/更新记忆。
- 过程中不懂不确定就问（不要停/结束后再问，过程中商量着来），多问少错。
- 不用 Agent。用 python（不是 python3）。
- 保证先前测试全过无回归。
- Godot：F:\Godot_4.6\Godot_v4.6-stable_win64.exe，每次测试用后即时关闭（用 timeout 240），否则爆内存。

# 工作流（批量推进，要快）
1. 先建绿基线：跑一次现有 run_tests 确认全绿（记忆说001-066已绿290 PASS，首跑验证）。
2. 一次性读完拆解_067-126.txt 头部 L19-24 + 各套装首张，然后跨套装一次性：
   - 确认 JSON part_067-126 effect_ids（可能有编号错位，仿套装9-11 python正则按part_id精确替换保留indent=2）；
   - 确认新机制依赖的既有件现状（DRAW_EQUIPMENT_AND_IMMEDIATELY_SET 现实现 / EXECUTE_SINGLE_MOVE free_move/max_cells 现状 / CHOOSE_INTEGER 现状 / 派生值 helper 现状 / 光环接入点 / 虚拟武器接入点 / restore_power 现状供 CANNOT_RESTORE_POWER 拦截 / 状态注册机制）；
   - 一次性收集 067-126 所有空白▶歧义 + 新机制方案列给我裁定（不要逐套装问，合并成一批）。
3. 拿到裁定后按套装顺序连续落码（12->21），每2-3套装跑 run_tests 保绿。可每2-3套装汇报一次（改了哪些文件/测试结果/遗留点），不必每套装都停下来等确认，除非遇到新问题。
4. 新机制（新op/原子动作/复杂交互/派生值/状态）落码前仍要先给我看方案（仿哪个既有件/登记在哪/参数/接入点），但同类机制合并确认（如 DRAW_EQUIPMENT_AND_CHOOSE_SET_OR_SELL 仿 DRAW_EQUIPMENT_AND_IMMEDIATELY_SET 一次确认；全场光环+虚拟武器派生值接入一次确认；EXECUTE_SINGLE_MOVE ignore_power/force_exact_cells 扩展一次确认）。
5. 用 TodoWrite 跟踪套装12-21进度。

# 落码规则（按拆解「实现预告」分类，继承001-066）
- **A 复用 effect_XXX**：核对现有代码与拆解一致；不同按新文本重构。
- **B 新建 effect_NNN**：GeneratedEquipmentEffects.gd build_equipment_effects() 末尾加 ActionEffect.new() 块，直接用拆解 conditions/target_rules/costs/actions 字面量，仿周围风格。
- **C 新条件 op**：ConditionChecker.gd check_single match 加分支，装备牌专用用 _equip_mech_id/_equip_player_id helper 取来源。
- **D 新原子动作**：ActionService.gd _is_atomic_action 列表 + _execute_atomic_action 特判 + _dispatch_atomic_action 分发 + record_keys(_create_action 注入record)。
- **E 新复杂交互**：TimingEngine.gd _execute_actions 拦截（仿 CHOOSE_ONE/CHOOSE_INTEGER/OFFER_DAMAGE_REDIRECT/DRAW_EQUIPMENT_AND_IMMEDIATELY_SET 挂起+resume_pending_effect phase）+ app_root 弹窗 + 面板。
- **F 派生值**：GeneratedEquipmentEffects.gd 加 compute_*/get_* helper；接入 MechState.get_armor/get_total_power 或 MechSlotState.get_effective_armor(mech)；不注册监听器；_is_derived_effect 加新id。
- **G 注册**：set_equipment_action.gd _register_equipment_effects 按 effect_ids 注册 permanent listener（binding_context 注入 card_instance_id/mech_id/player_id/slot_id——套装9已加slot_id）。
- **H UI**：DIRECT 主动效果->equipment_panel/skill_bar "发动"按钮（effect_017/019范本，**注意effect_040 DIRECT发动按钮遗留待补**）；新弹窗->app_root _show_popup + panel，PvP 路由复用 _popup_owner；CHOOSE_INTEGER 弹窗已实现（choice_panel列整数选项）。
- **I JSON**：data/cards/equipment_parts.json effect_ids 手维护（勿重新生成/勿改xlsx导出工具）。编号错位用 python 正则按 part_id 精确替换保留 indent=2，勿用 json.dump 重排。
- **测试**：tests/test_equipment_effects.gd 加 test_ 方法（run_tests.gd get_method_list 自动枚举）；复用 _new_battle/_ensure_equipment_in_hand/_ensure_action_card_in_hand/_pump_frames/_has_condition/_action_type_in_effect/_action_has_param(已递归CHOOSE_ONE)/_Action mock（Action.new()+registry.register+fire_timing+resume_pending_effect）；派生值测试直接调 _GeneratedEquipmentEffects 静态 helper；攻击流驱动仿 test_flash_counter_order.gd。每套装至少1个结构测试 + 关键新机制行为测试，不必逐场景覆盖，实机F3补。

# 关键文件落点（继承001-066 + 067-126新增）
| 改什么 | 文件 | 位置 |
|---|---|---|
| 装备效果定义 | scripts/generated_database/GeneratedEquipmentEffects.gd | build_equipment_effects() + 派生值helper区（card_damage_immune_armor_amount/slot_damage_threshold_power_bonus/compute_faction_*，新加 get_global_faction_equipment_aura_bonus/get_virtual_weapon_from_equipment） |
| 新条件op | scripts/action_core/ConditionChecker.gd | check_single match |
| 新原子动作/扩展 | scripts/action_core/ActionService.gd | _is_atomic_action(~L170) + _execute_atomic_action(~L290) + _dispatch_atomic_action(~L632) + record_keys（DRAW_ACTION.bind_result_to/SPEND_POWER.amount=&"ALL_CURRENT"等扩展） |
| 新复杂交互 | scripts/action_core/TimingEngine.gd | _execute_actions 拦截（DRAW_EQUIPMENT_AND_CHOOSE_SET_OR_SELL/REPEAT_SELF_DAMAGE_AND_FREE_MOVE）+ resume_pending_effect phase |
| EXECUTE_SINGLE_MOVE扩展 | scripts/action_defs/single_move_action.gd | ignore_power/force_exact_cells/max_cells=$variables.n（仿套装6 free_move） |
| 派生值接入 | scripts/runtime/MechState.gd / MechSlotState.gd | get_armor/get_total_power / get_effective_armor(mech)（全场光环接入） |
| 新状态CANNOT_RESTORE_POWER | scripts/effect_core/GameActions.gd(restore_power拦截) + TurnService(回合开始移除) + 状态注册 |
| 效果注册 | scripts/action_defs/set_equipment_action.gd | _register_equipment_effects + _is_derived_effect |
| JSON effect_ids | data/cards/equipment_parts.json | 每张牌 effect_ids |
| DIRECT主动UI | scripts/ui/equipment_panel.gd / skill_bar.gd | "发动"按钮（effect_017/019范本，补effect_040遗留） |
| 弹窗接线 | scripts/app/app_root.gd + scripts/action_core/ActionUIBridge.gd | _show_popup + _popup_owner（新弹窗复用choice_panel/integer_select模式） |
| 测试 | tests/test_equipment_effects.gd | run_tests.gd 已注册 |

# 关键坑（勿重复踩，详见记忆 effect-coder.md + 各suite记忆 + 001-066实现）
- **动力消耗走 spend_power 直调不发 STAT_MOD_SETTLE**（套装5 effect_017 教训）：067 effect_050 拆解监听 STAT_MOD_SETTLE + PAYLOAD_STAT_METHOD_IS spend，但移动消耗动力走 spend_power（GameActions原子）不发 STAT_MOD_SETTLE。需确认：要么仿 effect_017 改 BASIC_MOVE_AFTER + OWNER_POWER_EQUALS（记忆 suite-5 方案），要么确认 stat_modify 动作发 STAT_MOD_SETTLE 的场景。**这是 067-126 第一个需落码前确认的机制点**。
- **effect_fire DIRECT 触发**：record_keys 已含 phase；effect_fire._step_extract_info 注入 source_mech_id；_execute_effect_by_id 注入 binding_context（从 permanent listener entry）。effect_040 DIRECT 发动按钮遗留待补。
- **free_move/max_cells/adjacent_only**（套装6）：single_move 已支持（_remaining_power=max_cells不依赖动力/adjacent_only 检查_HexGrid.distance==1 需preload _HexGrid/basic_move 跳过spend_power）。067-126 的 ignore_power/force_exact_cells 是新扩展（仿 free_move，ignore_power 不依赖动力 + force_exact_cells 强制走满否则失败）。
- **DRAW_ACTION 用 player_id 非 target_id**（draw_action_cards 读 player_id），$binding_context.player_id 解析。
- **CHOOSE_INTEGER 已实现**（套装10）：_execute_actions 拦截 + _eval_expr（Expression 解析 $choice.n/$binding_context.owner_gold/算术/floor->int）+ resume_pending_effect choose_integer phase + AI自动选min + 人类choice_panel列整数选项。套装17 复用，max_value_expr/amount_expr/value_expr 按需。
- **优先级错开保序**（counter_e2=20先于flash_e2=10；近战头转换priority20先于威力+2 priority10；预判2=30先于掩护1=10）。
- **fire_timing 首循环 optional弹窗置waiting_timing会丢剩余同级监听器**；靠提优先级或走 waiting_effect_action 暂存路径规避。
- **弃置全量走 discard_card 动作**发 DISCARD_BEFORE/AFTER/SETTLE，reason 过滤防循环；离场诱发监听 DISCARD_AFTER（牌在 tmp_zone）。装备 listener 注销在 _step_settle（DISCARD_AFTER 后），勿改回 move_to_tmp。
- **损伤放置走 damage_change 动作**，DAMAGE_REDIRECT_WINDOW 一次性汇总转移窗；EXECUTE_DAMAGE_CHANGE fixed_slot 直接置X点（套装9已实现，跳过转移窗+逐点UI，_step_extract_info把offer_redirect timing置空）。
- **派生值型不注册监听器**，查询点实时重算；_is_derived_effect 列表需加 067-126 新派生值 id（全场光环/虚拟武器等）。
- **状态挂A时点fire在B时点时 target 用 $binding_context.xxx 非 $payload.xxx**。
- **effective_weapon_type**：武器类型效果读 attack.record.effective_weapon_type（近战头转换 priority20 改写）；基础武器虚拟ID frame_base_weapon_X 取 range 用 get_base_weapon。虚拟武器（套装21）接入武器选择/ATTACK_BEFORE。
- **离开手牌不再触发**：_listener_card_still_active 仅校验 permanent_while_in_hand 牌仍在 action_hand。
- **Q3守卫**（套装9）：CHOOSE_ONE branch_actions while串行，只上一个子动作是 fixed_slot EXECUTE_DAMAGE_CHANGE 时检测来源牌离场则停止剩余（不误杀 effect_019 弃自身换动力）。067-126 若有"置损伤致弃置+后续"同型效果，复用此守卫。
- **GDScript4 坑**：RefCounted .get(key,default) 只1参（用属性访问）；lambda标量按值捕获（断言用数组元素承载）；int==&"StringName"会崩先 String() 转；StringName比较用 String(a)==String(b)；**restore_power 用 str() 非 String()**（String 对某些类型 Nonexistent constructor，套装10修复）；Expression 解析 floor->int（套装10 _eval_expr）；Edit 匹配 box-drawing(═)/en-dash(--)易失败，用纯代码行做 old_string 锚点。
- **既有坑**：get_actions_by_type 返回 Action 对象数组（取id用 a.action_id）；ATTACK_MARKERS_ABOVE 严格>(threshold0=≥1) 而 MOVED_DISTANCE/POWER_SPENT_THIS_TURN_ABOVE 是 >=；_extract_stat_mod_params target_id 优先 params>payload.target_id>payload.mech_id>source_mech_id；payload = action.record.duplicate()（fire_timing L89，含 attack_action_id 等 record 字段）；binding_context 含 card_instance_id/mech_id/player_id/card_def_id/slot_id（套装9加slot_id）。
- **预存未修**（报备，非阻塞）：①remove_card_from_all_zones 弃置装备不把 card.damage_tokens 转 region_damage_tokens；②CardSetService.sell_equipment 卖已设置装备在 discard 前直接 unregister（离场效果在 sell 路径不触发）；③moved_cells_this_turn 未实现追踪（effect_012/013 的 MOVED_DISTANCE_THIS_TURN_ABOVE 只读不写，与 power_spent_this_turn 同型；若 067-126 有移动距离阈值效果需实现追踪）；④effect_040 DIRECT 发动按钮遗留。

# 命令
- 跑测试：`timeout 240 "F:/Godot_4.6/Godot_v4.6-stable_win64.exe" --headless --path . -s res://tests/run_tests.gd`（Godot用后即关）。
- 失败可自修；反复失败≥3轮停止自修，汇报问题清单给我。
- JSON 编号错位修正：python 正则按 part_id 精确替换保留 indent=2（仿套装9-11 fix_suiteN_json.py）。

# 开场
读完上述后，一次性读完拆解_067-126.txt 头部 L19-24 + 各套装首张 + 确认 JSON part_067-126 + 新机制既有件现状，然后跨套装一次性收集所有空白▶歧义 + 新机制方案列给我裁定，拿到裁定后按 12->21 连续落码（每2-3套装跑测试保绿，每2-3套装汇报一次）。全程多问少错，但要快。
